# Dependencies — Installed Tools & Why

Every tool/package gets a row **only after** it's been justified per [[01-Philosophy]] hard rule 3 (explain the problem → explain the tool → install → verify → smallest example → integrate) and actually installed. Nothing goes here speculatively. Referenced from [[00-Hub]] and [[02-Architecture]].

| Tool / Package | Version | Purpose | Introduced (roadmap version) | Date | Why (link to [[04-Decisions]] entry if applicable) |
|---|---|---|---|---|---|
| Git | 2.55 | Version control — sync the whole project (context vault + future code) between two machines with real history and conflict resolution, instead of manually copying files | pre-V0.1 (infrastructure, not a roadmap version) | 2026-08-20 | [[04-Decisions]] "Git + private GitHub repo chosen for multi-machine work" |
| Obsidian | (installed by user) | Renders the `context/` vault's `[[wikilinks]]` as a navigable graph | pre-V0.1 (infrastructure) | 2026-08-20 | [[04-Decisions]] "Obsidian chosen over Notion or plain markdown" |
| Python venv (`lab/.venv`) | Python 3.14.6 | Isolates this project's future `pip install`s from the system Python and any other project — created empty, no packages installed yet beyond bundled `pip` | V0.3 (first Python code — the raw HTTP server) | 2026-08-24 | Genuinely justified now per hard rule 4; previously removed from V0.1 for being premature, see [[04-Decisions]] "Removed premature venv step from V0.1" |
| FastAPI | 0.141.1 | Web framework — replaces `server.py`'s manual `if self.path == ...` routing and hand-built responses with decorator-based routing and automatic JSON serialization | V0.4 | 2026-08-25 | Contrasted directly against the raw `http.server` pain felt in V0.3, per hard rule 7 |
| Uvicorn | 0.52.4 | ASGI server — the program that actually listens on a port and hands requests to FastAPI; FastAPI itself only defines routing/logic, doesn't run standalone | V0.4 | 2026-08-25 | Standard pairing for any ASGI framework, explained as the same client/server split learned in V0.3 |
| PostgreSQL | 17 | Relational database server — gives `/models`/`/datasets`/`/experiments` real persistence instead of data hardcoded in Python that vanishes on restart | V0.5 | 2026-08-25 | Already installed and running locally (Windows service) before this project needed it — no fresh install required, just connecting a dedicated `ai_research_lab` database. See [[04-Decisions]] |
| psycopg[binary] | 3.3.4 | Python driver for PostgreSQL — translates Python calls into Postgres's wire protocol and back, same role `http.server`/`fetch()` played for HTTP | V0.5 | 2026-08-25 | Chosen over an ORM (SQLAlchemy) deliberately — understand the raw SQL/connection layer first, per hard rule 7 |
| python-dotenv | 1.2.3 | Loads `lab/.env` (gitignored) into environment variables, so the DB password never appears as literal text in a committed file | V0.5 | 2026-08-25 | Addressed *before* writing any connection code, not after — direct callback to the pain of the earlier git-history-rewrite saga |
| pandas | 3.0.5 | Structured tabular comparison across multiple datasets (word count, unique words per file) — a shape plain Python lists/dicts don't handle as naturally, and the standard tool that plugs directly into matplotlib | V0.8 | 2026-08-28 | Motivating problem: no way to compare all three real datasets side by side, only preview one at a time |
| numpy | 2.5.2 | Transitive dependency of pandas | V0.8 | 2026-08-28 | Installed automatically with pandas, not chosen directly |
| python-dateutil | 2.9.0.post0 | Transitive dependency of pandas | V0.8 | 2026-08-28 | Installed automatically with pandas |
| six | 1.17.0 | Transitive dependency of python-dateutil | V0.8 | 2026-08-28 | Installed automatically with pandas |
| matplotlib | 3.11.1 | Visualizing the Pandas comparison as an actual chart instead of reading numbers off a printed table | V0.8 | 2026-08-28/29 | Verified smallest-possible-example first (a 2-row fake DataFrame) before applying to real dataset files, per hard rule 3 |
| contourpy, cycler, fonttools, kiwisolver, packaging, pillow, pyparsing | various | Transitive dependencies of matplotlib | V0.8 | 2026-08-28/29 | Installed automatically with matplotlib |
