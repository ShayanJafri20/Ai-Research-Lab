# Next Step

The single current action. Overwrite this at the end of every session — it should always answer "what do we do the moment this reconvenes." Linked from [[00-Hub]].

## Right now

The context vault is on GitHub (`https://github.com/ShayanJafri20/Ai-Research-Lab`, branch `main`) — see [[04-Decisions]] and [[06-Dependencies]]. **V0.1 through V0.8's core loop are all complete.** The app is a real full stack: static HTML/CSS/JS frontend → `fetch()` → FastAPI backend → PostgreSQL + real files on disk + real Pandas analysis, all wired end-to-end.

**V0.8 summary (2026-08-28/29) — Pandas + EDA, wired into the live app, not left in a script:** installed `pandas` + `matplotlib` (with transitive deps — see [[06-Dependencies]]). New `lab/eda.py`: `analyze_dataset()` (word count, unique word count via a Python `set`), `top_words()` (`collections.Counter` + a regex to strip punctuation before counting), `clean_gutenberg_text()` (`str.find()` + slicing to strip Gutenberg's license header/footer). Real bug caught mid-session, not contrived: initial stats/word-frequency numbers were badly inflated by Gutenberg's boilerplate license text being read as part of "the story" — caught by testing `top_words()` on a hardcoded sample that turned out to *be* the license text, not the book. Fixed properly, not worked around. `eda.py`'s script-level code (the demo loop, `plt.show()` calls) was wrapped in `if __name__ == "__main__":` — a real, necessary fix, not a style nicety: without it, importing `eda.py` from `main.py` would have executed the entire demo script (including blocking on chart windows) every time the server started. New route `GET /datasets/{dataset_id}/stats` in `main.py` imports and reuses `analyze_dataset`/`top_words`/`clean_gutenberg_text` directly — first time this project imports one of its own local files as a module, not just installed packages. Frontend: new `.stats` div, the existing dataset-click handler extended to fetch both `/preview` and `/stats` and display both.

**A genuine, honest predict/observe moment on my end, not just the user's:** I predicted a plain `.py` script's plot workflow (run → window pops up → close it) would feel annoying enough to justify switching to Jupyter. User reported it felt completely fine — so Jupyter was correctly *not* introduced, per hard rule 3. Worth remembering next time a similar "this seems like it'll be annoying" guess comes up: verify it was actually felt before reaching for the "better" tool.

**Several real bugs this stretch, self-corrected with guidance, not just handed the fix:** an unused `n` parameter in `top_words` (defined but never passed to `.most_common()`); a duplicate function name (`get_dataset_preview` reused for the new stats route, a copy-paste artifact that also silently used the wrong path — missing leading `/` — and never actually called the EDA functions at all); and a JS variable-renaming mismatch (renamed `response`/`data` to `previewresponse`/`statsresponse` etc. per-fetch, but left some references pointing at the old, now-undefined names — a `ReferenceError` that silently stopped the whole click handler). Also hit a real caching false alarm: after fixing the JS, stats still didn't show until a hard refresh (`Ctrl+F5`) — not a code bug, browser cache outlasting Live Server's reload. Full detail in [[05-Concepts]].

**Ongoing correction from V0.7, reinforced again this stretch:** user continues to want backend/Python work done by them, not defaulted to AI — logging/error handling extension, most of `eda.py`, and the stats route were all attempted by the user first, with the AI writing directly only when explicitly asked ("you do it").

**Next action: continue V0.8, or move to V0.9 (ETL).** Not yet done within V0.8, if continuing: extend word-frequency/stats to all three datasets in the UI at once (currently only Peter Rabbit gets analyzed by the standalone `eda.py` demo — the live `/stats` route works for any `dataset_id` already, so this is really just a UI/UX extension, not new backend work), or a proper stopword-filtered word-frequency chart (right now `top_words` is dominated by "the"/"and"/"he" — a real, deferred cleaning step, not yet asked for). If moving on: V0.9 is ETL — Extract (pull data from somewhere), Transform (clean it — much of `eda.py`'s cleaning logic already IS this), Load (into Postgres) — and is also the direct prerequisite for the now-firmly-planned Power BI step right after it.

**If picking this up on a different PC:** clone the repo, set git identity there, `python -m venv .venv` inside `lab/` then `pip install -r requirements.txt` (now includes pandas/matplotlib and their dependencies). PostgreSQL 17 must be installed separately (a real server install, not `pip`). Build the DB: create it empty, run both migrations via `psql -f`, then `psql -f seed.sql` for real starting data. `lab/.env` needs creating locally from `lab/.env.example` with that machine's own DB password. `git pull` before starting, `git add . && git commit && git push` at the end.

Do not install Docker or Redis yet — not justified until a real problem calls for them (hard rule 3).

## After that

Update this file, [[02-Architecture]], [[03-Roadmap]] "current position," [[06-Dependencies]], and [[05-Concepts]] before ending the session.
