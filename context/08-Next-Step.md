# Next Step

The single current action. Overwrite this at the end of every session — it should always answer "what do we do the moment this reconvenes." Linked from [[00-Hub]].

## Right now

The context vault is on GitHub (`https://github.com/ShayanJafri20/Ai-Research-Lab`, branch `main`) — see [[04-Decisions]] and [[06-Dependencies]]. **V0.1 through V0.9 (Load-scoped) are all complete.** The app is a real full stack: static HTML/CSS/JS frontend → `fetch()` → FastAPI backend → PostgreSQL, with real files on disk, real Pandas analysis, and now permanently stored (not recomputed-per-request) stats.

**V0.8 summary (2026-08-28/29) — Pandas + EDA, wired into the live app:** installed `pandas` + `matplotlib` (see [[06-Dependencies]]). `lab/eda.py`: `analyze_dataset()`, `top_words()` (`collections.Counter` + regex tokenizing, later filtered through a hardcoded `STOPWORDS` set + list comprehension so results show real content words instead of "the"/"and"/"he"), `clean_gutenberg_text()` (`str.find()` + slicing to strip Gutenberg's boilerplate, added after discovering it was inflating every stat). Script-level code guarded by `if __name__ == "__main__":` so importing the module doesn't execute the demo/plotting code. **Honest AI-side predict/observe moment:** predicted a plain script's `plt.show()` workflow would feel annoying enough to justify Jupyter — it didn't, so Jupyter was correctly not introduced, per hard rule 3 applied to the AI's own guesses too.

**V0.9 summary (2026-08-29) — ETL, scoped to Load only, done for real:** deliberately skipped Extract automation (no repeated pain yet — only added datasets manually 3 times, logged to [[09-Ideas-Backlog]]) and Transform (already existed in `eda.py`). Built the missing piece: migration `0003_add_dataset_stats.sql` creates `dataset_stats` (`dataset_id` FK, `word_count`, `unique_words`, `top_words JSONB` — first use of Postgres's JSON column type in this project). New one-time script `lab/load_stats.py` computes stats for all 3 datasets and `INSERT`s them — first real use of `conn.commit()` in this project (writes need it explicitly; reads never did), the actual live application of the V0.6 sandbox's COMMIT/ROLLBACK concept. `main.py`'s `/datasets/{id}/stats` route rewritten to a plain `SELECT` from `dataset_stats` instead of re-reading and re-analyzing the file on every request — confirmed via `curl`, real stopword-filtered content words coming straight from the table. Unused `eda` import removed from `main.py` once the route no longer needed it.

**Also this stretch — a real BI-tooling scoping conversation, not just code:** user described seeing "Power BI Embedded" at a friend's company (charts inside another app, Microsoft login) — clarified this needs Azure AD + a paid Power BI Pro/Premium tier, genuinely bigger than the already-planned "connect Power BI Desktop to the database" and logged separately to [[09-Ideas-Backlog]] rather than conflated with it. **Metabase added as a second, firmly-planned tool alongside Power BI** (same V0.9-completion trigger) — free, open-source, browser-based, covering the "charts in an actual webpage" need Power BI Desktop's separate app doesn't. See [[01-Philosophy]] and [[03-Roadmap]].

**Several real bugs this stretch, self-corrected with guidance:** an unused `n` parameter in `top_words`; a duplicate function name + wrong path + copy-pasted logic that never called the EDA functions at all (the first stats-route attempt); a JS variable-renaming mismatch causing a silent `ReferenceError`; a real caching false alarm (hard refresh needed, not a code bug); an extra SQL placeholder (5 `%s` for 4 parameters) in `load_stats.py`; and a table-name typo (`datasets_stats` vs. the actual `dataset_stats`) that would have surfaced right after the first fix. Full detail in [[05-Concepts]].

**Ongoing correction from V0.7, reinforced again this stretch:** user continues to want backend/Python work done by them, not defaulted to AI — the migration, `load_stats.py`, and the rewritten `/stats` route were all attempted by the user first.

**Next action: finish the BI tooling in progress, then start V1.0.** Concretely, resume with:
1. **Power BI Desktop** — installed, connected to `ai_research_lab`, data loaded. Not yet done: build the first chart (Clustered Column, `dataset_stats.dataset_id` on X-axis, `word_count` on Y-axis — see [[05-Concepts]] for the exact steps already given), then try swapping in `datasets.filename` for readable labels via the auto-detected FK relationship.
2. **Metabase** — installed and running (`C:\Tools\Metabase\metabase.jar`, Java 25 JRE at `C:\Program Files\Eclipse Adoptium\jre-25.0.4.101-hotspot`), but the setup wizard hasn't been started yet. Resume at `http://localhost:3000/setup/` (restart with `java -jar metabase.jar` from that folder if the machine restarted since 2026-08-29) — create the local admin account, then connect it to the same `ai_research_lab` Postgres database (same host/port/credentials as everywhere else in this project).

Once both tools have at least one real chart working, **V1.0 (RNN — the NLP research track begins)** is next — a genuinely bigger step than anything so far, the actual ML/research side of this project starting for real, distinct from the web/data-engineering foundation built through V0.9. Per hard rule 3, start V1.0 by identifying the real problem (feeding real text — the Gutenberg datasets already sitting in `lab/datasets/` — into a sequence model) before picking any tool, and check [[03-Roadmap]]'s V1.0-V1.3 concept checklist (tokenization first, PyTorch only once manual backprop is genuinely painful) before writing anything.

Lower-priority, still open: Extract automation (if adding datasets ever becomes frequent enough to be annoying) and the deliberately-deferred `conn.close()`-on-exception connection leak — both logged in [[09-Ideas-Backlog]], no urgency.

**If picking this up on a different PC:** clone the repo, set git identity there, `python -m venv .venv` inside `lab/` then `pip install -r requirements.txt` (now includes pandas/matplotlib and their dependencies). PostgreSQL 17 must be installed separately (a real server install, not `pip`). Build the DB: create it empty, run all three migrations in order via `psql -f`, then `psql -f seed.sql` for starting data, then `python load_stats.py` to populate `dataset_stats` (seed.sql doesn't cover this — it's computed, not static seed data). `lab/.env` needs creating locally from `lab/.env.example` with that machine's own DB password. `git pull` before starting, `git add . && git commit && git push` at the end.

Do not install Docker or Redis yet — not justified until a real problem calls for them (hard rule 3).

## After that

Update this file, [[02-Architecture]], [[03-Roadmap]] "current position," [[06-Dependencies]], and [[05-Concepts]] before ending the session.
