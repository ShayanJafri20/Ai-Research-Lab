# Concepts — Knowledge State Tracker

Update this whenever a session reveals what the user does/doesn't understand. Drives how deep explanations in [[01-Philosophy]] hard rule 2 need to go. Referenced from [[00-Hub]].

## Understood (prior background — cover it anyway, just move faster)

- Python, NumPy, Pandas, scikit-learn, PyTorch basics
- Core ML/DL concepts, model training loop
- CNN architectures: AlexNet, ResNet
- NLP/Transformer concepts, attention (conceptual level — depth to confirm when V1.0–V1.3 is reached)

**Note (2026-08-20, updated 2026-08-20):** "Understood" does not mean skip. User wants full coverage — basics included — for AI/ML topics when the roadmap reaches them, not just software engineering content; reinforcing known material strengthens understanding rather than wasting time ("it's better to look at everything"). The only thing "understood" changes is pacing: basics move faster as a refresher instead of a first exposure, and depth (math, tensor shapes, implementation) still gets covered even on architectures already used in practice. Treat the depth side the same as [[01-Philosophy]] hard rule 7 (one layer below the current abstraction).

## Partially understood

*(none logged yet)*

## Struggling / weak — treat as new even if previously exposed

- General software engineering fluency (self-reported): writing moderately complex code without heavy debugging, problem decomposition, reading unfamiliar code, software architecture
- Standard programming constructs not yet confirmed: decorators, classes/inheritance, generators/iterators, context managers, modules/packages, virtual envs, async/await, closures, type hints, HTTP, processes/threads, event loops — see [[01-Philosophy]] hard rule 1

## Log

| Date | Concept | Status | Notes |
|---|---|---|---|
| 2026-08-25/27 | Relational databases (tables/rows/columns, `SERIAL PRIMARY KEY`, `NOT NULL`, auto B-tree indexing), SQL (`CREATE TABLE`/`INSERT`/`SELECT`), Python↔Postgres via psycopg (`connect`/`cursor`/`execute`/`fetchall`), environment variables/`.env` for secrets, list comprehensions (connected to JS `.map()` as the same underlying idea) | Understood, confirmed end-to-end | Wrote `CREATE TABLE`/`INSERT`/`SELECT` by hand in pgAdmin before any Python. Asked good clarifying questions unprompted ("is this coming from the DB?"), and the live-insert proof (adding a row via `psql` and seeing it appear via the API with zero code changes) landed well as genuine verification, not just taking it on faith. Independently proposed extending the `/models` pattern to `/datasets`/`/experiments` once shown once — correctly predicted "the others will come from the return statement" before checking. Explicitly and reasonably declined a mid-build refactor (`get_connection()`) to avoid overloading while still learning the core feature — see [[01-Philosophy]] "Refactor timing" and [[04-Decisions]]. |
| 2026-08-25 | FastAPI (`FastAPI()` instance, `@app.get` decorator routing, auto JSON serialization), Promises/`async`/`await`/`fetch()`/`response.json()`, CORS (origin = protocol+host+port, Same-Origin Policy, `Access-Control-Allow-Origin`) | Understood | Correctly connected `app = FastAPI()` to the earlier `server = HTTPServer(...)` pattern unprompted-adjacent (recognized "instance" reuse once pointed out). Hit a real, unplanned CORS error live (Live Server on :5500 fetching FastAPI on :8000) and it was used as the actual teaching moment rather than pre-explained — matches hard rule 5. `async`/`await`/decorators were genuinely new (hard rule 1 list) and taught conceptually before any code, same pattern that worked for HTTP fundamentals before `server.py`. |
| 2026-08-24 | Python classes/inheritance/methods (`class Handler(BaseHTTPRequestHandler)`, `def do_GET(self)`), raw HTTP server (`http.server`), manual path-based routing, status codes 200/404 in practice | Understood | Same pattern as the HTML skeleton: first attempt moved too fast (built the whole `server.py` in one pass, user wrote nothing) — explicitly called out ("YOU DID ALL THAT WITHOUT ANYTHING I KNOW"), redone one line at a time with the user typing every line themselves, confirmed correctly ("basically we are doing inheritance" — self-identified the concept unprompted). Also correctly diagnosed a real bug independently (routing appeared broken, traced to the edit never having been saved, not a server/caching issue). One indentation slip along the way, self-corrected. |
| 2026-08-21 | HTML skeleton (doctype, `<html>`, `<head>`/`<title>`, `<body>`) | Partially understood | Couldn't write it unassisted on first try ("I don't know the code") — built together line by line with explanation per line. Predict-run-inspect succeeded afterward (correctly matched tab vs. page content on both machines). Re-attempt writing structure unassisted next time a similar skeleton is needed, before calling this fully understood. |
| 2026-08-20 | Git core mechanics (working directory / staging / repo, `add`/`commit`/`push`/`pull`, `.gitignore`) | Understood | Confirmed after a from-scratch walkthrough — first pass moved too fast (jumped straight to commands without the mental model), second pass slowed down and it landed. Branches/merges/PRs still unconfirmed — see [[01-Philosophy]] hard rule 3, deferred to first real multi-machine divergence. |
| 2026-08-21 | Git merge conflicts (real, not staged) | Resolved, not yet taught | Two parallel Claude Code sessions (this one and one on the other PC) both independently pushed vault edits without either pulling first, causing a genuine divergence in `02-Architecture.md`/`03-Roadmap.md`. Resolved by fetching, merging, hand-resolving the conflict markers (both sides' content was complementary, kept both), and pushing the merge commit — but this was done *for* the user, not explained step-by-step yet. Owe a proper walkthrough of what a conflict marker means and why it happens next time it's relevant. Practical takeaway to state explicitly: avoid running two Claude Code sessions on this project across machines without a pull in between. |
| 2026-08-20 | (vault created, no teaching session yet) | — | — |
| 2026-08-21 | HTML basics: `<!DOCTYPE html>`, `<html lang>`, `<head>`, `<title>`, `<body>`, `<h1>` | Taught, re-explained | Skeleton was built in a prior session but not retained by the next one ("forgot about this html code") — re-taught chunk by chunk with MDN links. Watch for retention on a third pass before marking fully Understood. |
| 2026-08-21 | `<section>`/`<h2>`/`<button>` added to `lab/index.html`; user attempted the skeleton but wasn't confident writing it blind, so it was written directly and explained chunk by chunk (hard rule 6 escalation) | Taught | |
| 2026-08-21 | Misconception caught and corrected: "HTML does nothing without CSS/JS." Rendered page in browser and saw default bold/bordered styling with zero authored CSS — corrected via the user-agent stylesheet concept (browser ships default CSS for every tag) and the distinction between browser-default vs. custom behavior for JS. Good sign: user is reasoning from what they actually observe in the browser (hard rule 5), not just accepting explanations. | Corrected | Natural lead-in to the CSS phase — the real problem CSS solves is overriding these generic defaults, not "adding style from nothing." |
| 2026-08-21 | External stylesheet linking: `<link rel="stylesheet" href="...">`, three ways to attach CSS (inline/internal/external) | Attempted solo, correct | User wrote the `<link>` tag and created the empty `lab/style.css` file themselves without help — first fully independent edit this project (hard rule 6 ratio moving the right direction). |
| 2026-08-21 | DevTools Network tab; HTTP status codes preview (`200` vs `304 Not Modified`, browser caching) | Introduced, live example seen | Predicted `200`, actually saw `304` on a repeat refresh — used as a real predict/observe/explain moment (hard rule 5) rather than a mismatch to gloss over. Early preview of concepts formally scheduled for V0.3 in [[03-Roadmap]]'s HTTP checklist; revisit there for full depth (headers, request/response anatomy, status code families). |
| 2026-08-22/23 | CSS: margin-top vs margin-bottom placement, margin collapsing (incl. asymmetric case, 40/60 → 60 not 100), class selectors (`.classname`), cascade vs. specificity in practice (`.Datasets` overriding only one property, not the whole `section` rule), `display: block` default explaining full-width boxes, `font-family` + inheritance (text properties inherit, box-model properties don't), form controls not inheriting font by default | Understood, self-verified | Strong session — user predicted collapsing behavior correctly before checking (40/40 → 40, 60/40 → 60, not summed), asked good clarifying questions unprompted ("why does it take a full box," "what was cascading" before moving to inheritance), and is now writing CSS rules independently without being handed the syntax. Ratio (hard rule 6) trending well. |
| 2026-08-23 | Flexbox: `display: flex`, default `flex-direction: row`, `gap`/`column-gap`, margin collapsing does not apply to flex items, `<div>` as a non-semantic wrapper (vs. `<section>`) | Taught, applied correctly, understood the "why" | User wrapped the three sections in a `<div class="sections">`, applied `display: flex` and `gap` correctly, and independently reasoned through *why* collapsing no longer applied once flex was introduced rather than just accepting it. |
| 2026-08-23 | CSS wrap-up: `justify-content`, `font-size` + `rem` units (and why `rem` > `px` for accessibility), `position: fixed` (floating button pattern), `@media` queries/responsive breakpoints, `:hover` pseudo-class, `transition`, formal specificity point system | Understood, mostly self-verified | Correctly predicted `gap` + `justify-content: space-between` would compose rather than conflict — wrong on the *first* guess ("won't work together") but reasoned through the actual mechanism once shown the result, which is the more important skill. CSS Grid and `inline`/`inline-block` deliberately deferred, logged in [[09-Ideas-Backlog]] rather than forced in without a real need. |
| 2026-08-23 | JavaScript foundation (first real session): `const`/`let`, primitive types + arrays/objects, the `typeof` array quirk (`"object"`) and `Array.isArray`, zero-indexing, dot notation, `for...of` and classic indexed `for`, block scoping/shadowing, functions with parameters/`return`, template literals, callbacks, `addEventListener`, `.textContent`, `if`/`else`, `document.createElement`/`.appendChild`/`.append` — culminating in a full data-array → loop → function → DOM-render pattern, applied to all three sections (Models taught, Datasets done solo, Experiments requested done directly) | Strong first pass, some real bugs self-corrected | Hit and worked through three genuine bugs, not contrived ones: `button :hover` (space turning it into a descendant combinator, silently breaking all button styling), `textContent` assignment placed outside the callback's `{ }` (ran once on load instead of on click), and calling the wrong one of two similarly-named functions (`describeModelName` vs `describeModelNames`, side-effect vs. return-value confusion) — all diagnosed with real understanding of *why*, not just told to fix. Original V0.1 goal ("a button that changes something on the page") is now genuinely met, plus a real data-rendering pattern beyond the original spec. |
| 2026-08-24 | V0.2 complete: `classList.add`/`.remove`/`.toggle`, the event object + `event.target`, event bubbling/delegation (one listener on a parent handling clicks from dynamically-created children), refactoring duplicated code into a shared function (`renderList`) + `Array.prototype.map`, destructuring (array + object), spread/rest | Understood, one real bug repeated, one correction needed | Placed `button.classList.toggle(...)` outside the click callback's `{ }` — the *exact same bug shape* as the earlier `textContent` mistake (code outside `{ }` runs once on load, not on click) — diagnosed correctly once shown, which is the more important signal than not making the mistake at all. After writing `renderList` to replace three duplicated loops, forgot to delete the old ones, causing every list to double (6 items instead of 3) — caught by actually checking the browser rather than assuming. When asked to remove the dead code, user commented it out instead of deleting it, despite git already making deletion safe — corrected explicitly (rule: delete, don't comment out, trust version control) and user complied on request rather than doing it themselves. |
| 2026-08-27 | Backend refactor: extracting duplicated code into a shared Python function (`get_connection()`, same pattern as `renderList`) | Concept understood from the JS precedent; execution done directly by AI on user's explicit request ("you do it") rather than attempted solo | User recognized and named the parallel to `renderList` unprompted. Deviated from hard rule 6's usual "let the user attempt it" only because the user explicitly asked for it to be done directly — logged as a one-off, not a standing change to the rule. |
| 2026-08-28 | V0.7 (Dataset Explorer): FastAPI path parameters (`{dataset_id}: int`), parameterized SQL (`%s` placeholders, why never to build queries via string interpolation), `os.path.join`, `open()`/`with` as a context manager, `.split()`/slicing/`.join()` for a word-count preview, `HTTPException` + real HTTP status-code reasoning (4xx = client's fault, 5xx = server's fault), Python `try`/`except` layered on top of an explicit precondition check (two complementary, not redundant, error-handling styles), the `logging` module vs. `print()` | Real understanding demonstrated, mixed independence | Diagnosed a genuine `500` from a SQL/Python mismatch (`SELECT filename` but code assumed `row[1]` existed) correctly once shown; also hit the exact same "code outside `{ }` runs immediately" bug shape a third time (missing template-literal backticks) and a new one (editing a shared function broke sibling callers) — both self-caught once flagged. **Mid-session correction, worth treating as a standing update, not a one-off:** user explicitly pushed back on the AI implementing logging/error-handling directly ("we will do this all ourselves") — this extends the hard-rule-6 course-correction beyond CSS/frontend into backend/Python work, where a lot of direct implementation had crept in (venv/DB setup, refactors, this session's initial error-handling pass). Applied the correction immediately: reverted the AI-written fix, user rewrote it with guided hints, and asked good clarifying questions along the way (e.g. why `try`/`except` differs from `os.path.exists`, why `500` specifically). Also independently noticed a real, valid gap unprompted: placeholder `models` mixed CNN/vision names into a project whose actual near-term roadmap is pure NLP — a genuine architectural catch, not something flagged first by AI. |
| 2026-08-27/28 | V0.6: migrations as version-controlled `.sql` files (numbered/ordered, why `CREATE TABLE` isn't idempotent so re-running an already-applied migration fails), `ALTER TABLE ADD COLUMN`, foreign keys/`REFERENCES` and referential integrity, `psql -f` to run a file against a real database (walked through cmd.exe step-by-step, including the password-prompt-shows-nothing behavior), `UPDATE ... SET ... WHERE` (and the "no WHERE clause updates every row" danger), `JOIN ... ON` across three tables, indexes/B-trees (why `SERIAL` implies one, `EXPLAIN ANALYZE`), transactions/atomicity (`COMMIT`/`ROLLBACK`) | Strong session — real gap self-identified, syntax mostly correct on first attempt, one real bug found via predict-run-inspect | User independently wrote `0001_init.sql` correctly from a described schema, and asked *why* the app's basic-looking Models/Datasets/Experiments sections matter for later ML-heavy versions — a genuine architectural question, not just syntax — leading to documenting the section→future-version mapping in [[04-Decisions]]. On `0002`, predicted (before running) that missing semicolons would break multi-statement execution, then it did — real diagnostic instinct being applied, not just told the fix afterward. Needed heavy step-by-step help actually *running* `psql` from a terminal (didn't know `Ctrl+\``, cmd vs PowerShell syntax differences) — terminal/CLI basics are a real gap to keep in mind, distinct from SQL/concept understanding which was solid. Indexes and transactions requested as direct demos ("show me directly") rather than attempted solo, reasonable given they were explored as Sandbox concepts, not something being built into the real project. |
| 2026-08-28/29 | V0.8 (Pandas + EDA): `pd.DataFrame` from a list of dicts vs. a list of tuples (`columns=` needed for the latter), the DataFrame index, `os.path.basename`, `set()` for counting distinct items, `collections.Counter` + `.most_common(n)`, basic regex via `re.findall` for punctuation-stripped word tokenizing, `str.find()` + slicing to strip known boilerplate, `if __name__ == "__main__":` and *why* (import side effects — top-level code runs on import, not just on direct execution), importing a local project file as a module (`from eda import ...`) | Real understanding, several self-corrected bugs, one AI prediction proven wrong | Genuinely valuable moment: user's own test of `top_words()` (on a hardcoded license-text sample) accidentally revealed that every stat computed so far was inflated by Gutenberg's boilerplate — a real data-quality catch from testing, not something flagged in advance. Correctly predicted "the"/"of"/"you" would dominate raw word frequency before checking. Bugs hit and mostly self-diagnosed: unused `n` parameter (`.most_common()` called without it), a duplicate function name + missing leading `/` + copy-pasted logic that never actually called the EDA functions (escalated to "you do it" after a partial attempt), a JS variable-rename left half-applied (`response`/`data` still referenced after renaming to per-fetch names) causing a silent `ReferenceError`. **AI's own prediction disproven, logged honestly rather than glossed over:** predicted a plain `.py` script's `plt.show()` workflow would feel like enough friction to justify switching to Jupyter — user reported it felt completely fine, so Jupyter was correctly not introduced, matching hard rule 3's "only when actually felt" standard applied to the AI's suggestions too, not just the user's assumptions. |
